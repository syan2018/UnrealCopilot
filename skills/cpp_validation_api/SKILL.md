---
name: cpp_validation_api
description: CppSkillApiSubsystem 验证原语（蓝图编译摘要）
tags: [cpp, validation]
---

# CppSkillApiSubsystem - ValidationOps

本 skill 文档描述 `UCppSkillApiSubsystem` 的基础验证原语。

## 入口说明

从 UE Python 中获取子系统：

```
api = unreal.get_editor_subsystem(unreal.CppSkillApiSubsystem)
```

详细接口见 `docs/overview.md`。

